name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install SDK + self-contained server
        run: |
          python -m pip install --upgrade pip
          pip install -e "./sdk[dev]"
          pip install -e "./server[dev]"
      - run: ruff check server/app sdk/agenttrace examples server/tests sdk/tests
      - run: ruff format --check server/app sdk/agenttrace examples server/tests sdk/tests

  sdk-test:
    name: SDK Tests (standalone)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./sdk
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install SDK
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run tests with coverage report
        run: python -m pytest tests/ -q --cov=agenttrace --cov-report=term
      - name: Build SDK package
        run: |
          pip install build twine
          python -m build
          twine check dist/*

  server-test:
    name: Server Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install self-contained server
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run tests with coverage report
        run: python -m pytest tests/ -q --cov=app --cov-report=term

  dashboard-build:
    name: Dashboard Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./dashboard
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: npm test
      - run: npm run lint
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e -- --project=chromium

  evidence:
    name: Offline Evidence
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install project extras
        run: |
          python -m pip install --upgrade pip
          pip install -e "./sdk[dev]" -e "./server[dev]"
      - run: python scripts/portfolio_demo.py
      - run: python scripts/verify_portfolio_evidence.py

  forbidden-dependencies:
    name: Forbidden Dependency Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan source and operations files
        run: python scripts/check_forbidden_dependencies.py

  package-install:
    name: Wheel Install Without External Core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Build wheels
        run: |
          python -m pip install --upgrade pip build
          python -m build sdk
          python -m build server
          python scripts/check_wheel_contents.py
      - name: Install wheels in an isolated environment
        run: |
          python -m venv /tmp/agenttrace-wheel-venv
          /tmp/agenttrace-wheel-venv/bin/pip install sdk/dist/*.whl server/dist/*.whl
          /tmp/agenttrace-wheel-venv/bin/python -c "import agenttrace; import app.main; import app.config; import app.db; import app.internal.vendor_core.pricing"

  docker-build:
    name: Docker Build Verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build server image
        run: docker build -t agenttrace-server ./server
      - name: Build dashboard image
        run: docker build -t agenttrace-dashboard ./dashboard
